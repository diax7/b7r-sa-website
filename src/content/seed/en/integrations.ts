/** The integration tiles in English: the platform's name by slug (the Latin name is the brand's own). */
export const integrationsEn: Record<string, { name: string }> = {
  salla: { name: 'Salla' },
  zid: { name: 'Zid' },
  shopify: { name: 'Shopify' },
};
