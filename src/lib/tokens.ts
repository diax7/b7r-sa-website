/**
 * The few brand values that must exist outside CSS (viewport theme colour, the manifest and
 * OG images in Phase 1c). Mirrors `--color-primary` in `src/styles/globals.css`; keep in sync.
 */
export const BRAND_PRIMARY_HEX = '#0058B0';
